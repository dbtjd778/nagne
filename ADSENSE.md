# 구글 애드센스 심사 준비

이 문서는 나그네 사이트를 애드센스에 신청하기 전 점검할 항목을 정리한 것입니다.
정책은 수시로 바뀌므로 신청 직전에
[Google 게시자 정책](https://support.google.com/publisherpolicies/answer/10502938)과
[AdSense 프로그램 정책](https://support.google.com/adsense/answer/48182)을 한 번 더 확인하세요.

---

## 1. 이 사이트의 가장 큰 위험 요소

애드센스 거절 사유 중 압도적으로 많은 것이 **«가치 있는 콘텐츠 없음(Low value content)»**
입니다. 링크 모음 디렉토리는 이 판정을 받기 가장 쉬운 유형입니다. 심사자가 보는 것은
"링크 208개"가 아니라 "이 사이트가 원본으로 만들어낸 것이 무엇인가"이기 때문입니다.

현재 코드에 반영해 둔 대응은 다음과 같습니다.

| 위험 | 대응 |
| --- | --- |
| 링크만 있고 원본 텍스트가 없음 | 사이트마다 직접 쓴 한 줄 설명 208개, 카테고리마다 3문장 소개문 12개, 홈의 «선정 기준» 섹션, 소개 페이지 |
| 본문이 클릭해야만 나타남 | `/c/:categoryId` 카테고리 단독 페이지 12개 — 목록 전체가 처음부터 마크업에 존재 |
| 페이지 수가 너무 적음 | 총 17개 URL (홈 + 카테고리 12 + 소개/문의/개인정보/이용약관) |
| 크롤러가 페이지를 못 찾음 | `sitemap.xml` 자동 생성, 푸터에서 12개 카테고리 전부로 내부 링크 |

**그래도 부족할 수 있습니다.** 승인 확률을 실질적으로 올리려면 아래 둘 중 하나는
하는 편이 좋습니다.

- **읽을거리 추가** — "심심할 때 볼 사이트 10곳" 같은 큐레이션 글을 5~10편 정도.
  카테고리 페이지와 달리 문단이 있는 글이 심사에서 훨씬 유리합니다.
- **프리렌더링 도입** — 현재는 SPA 라 초기 HTML 이 비어 있습니다. 구글 크롤러는
  JavaScript 를 실행하지만 항상 보장되지는 않습니다. 빌드 시 각 라우트를 정적
  HTML 로 미리 렌더링하면 이 불확실성이 사라집니다.

---

## 2. 신청 전 체크리스트

### 필수

- [ ] **도메인 구입 및 연결** — 서브도메인(`*.netlify.app`)으로도 신청은 되지만
      자체 도메인이 훨씬 유리합니다. 최소 며칠 이상 운영된 상태가 좋습니다.
- [ ] `src/site.config.js` 의 `url` 을 실제 도메인으로 변경
- [ ] `src/site.config.js` 의 `email` 을 실제로 받는 주소로 변경
      (심사 중 연락이 갈 수 있고, 개인정보 문의 창구로도 필요합니다)
- [ ] `index.html` 의 `<link rel="canonical">` 과 `og:url` 도메인 변경
- [ ] `npm run build` 실행 → `sitemap.xml`, `robots.txt` 가 새 도메인으로 갱신되는지 확인
- [ ] **개인정보처리방침 페이지** (`/privacy`) — 이미 작성되어 있습니다.
      쿠키 사용, Google 의 제3자 광고 쿠키, 맞춤 광고 해제 방법이 모두 포함되어 있어야
      하며 현재 포함되어 있습니다.
- [ ] **문의처** (`/contact`) 와 **소개** (`/about`) 페이지 — 작성 완료
- [ ] robots.txt 가 크롤러를 막고 있지 않은지 확인 (특히 `Mediapartners-Google` 차단 금지)
- [ ] 깨진 링크 정리 — `npm run check-links` 로 확인 (아래 3번 참고)

### 승인 후

- [ ] `index.html` 의 애드센스 스크립트 주석 해제 + `ca-pub-XXXXXXXXXXXXXXXX` 교체
- [ ] `public/ads.txt` 의 주석 해제 + 게시자 ID 교체
      → 배포 후 `https://도메인/ads.txt` 로 열리는지 반드시 확인
- [ ] Google Search Console 에 도메인 등록 + `sitemap.xml` 제출
- [ ] 광고 단위를 어디에 넣을지 결정 (카테고리 페이지 목록 중간이 자연스럽습니다)

### 하면 안 되는 것

- 본인 광고 클릭, 지인에게 클릭 요청, 클릭 유도 문구
- 트래픽 구매나 자동 유입 프로그램
- 광고를 콘텐츠처럼 보이게 배치하거나 버튼 근처에 붙이기
- 심사 중 사이트를 비워두거나 «준비 중» 상태로 두기

---

## 3. 링크 상태 점검 결과

`npm run check-links` 로 208개 링크를 확인한 결과입니다.
(2026년 8월 기준, 실행 환경의 네트워크 상태에 따라 결과가 달라질 수 있습니다.)

### 응답이 404 인 곳 — 수정 또는 삭제 필요

| 이름 | 등록된 주소 | 비고 |
| --- | --- | --- |
| Drive & Listen | `driveandlisten.herokuapp.com` | Heroku 무료 플랜 종료. 현재 주소는 `driveandlisten.com` |
| 만다라트 | `mandal-art.web.app` | 404 |
| 텍스트 대치 메이커 | `text-replace.vercel.app` | 404 |
| Cheongnyeondaum | `dbtjd778.github.io/cheongnyeondaum-club` | 404 — 저장소 Pages 설정 확인 필요 |
| Weaone | `dbtjd778.github.io/weaone` | 404 — 저장소 Pages 설정 확인 필요 |

### 도메인이 조회되지 않는 곳 (DNS 실패) — 주소 재확인 필요

`tajawang.com`, `font.toss.im`, `jjal.studio`, `kwordle.kr`, `vrew.voyens.com`,
`speller.cs.pusan.ac.kr`

Vrew 의 공식 주소는 `vrew.ai` 입니다. 나머지는 실제 브라우저에서 한 번씩 열어보고
살아 있으면 그대로 두시면 됩니다 (점검 스크립트의 네트워크 환경 문제일 수 있습니다).

### 연결이 거부되거나 끊긴 곳 — 브라우저로 직접 확인

`sadari.net`, `emojiwiki.com`, `noisli.com`, `balancegame.net`,
`donothingfor2minutes.com`, `weavesilk.com`

### 정상이지만 봇을 차단하는 곳 (문제 없음)

ChatGPT, Claude, Unsplash, Pexels, TED, Stack Overflow 등 30곳은 자동화 요청을
403 으로 막을 뿐 실제로는 정상 동작합니다.

---

## 4. 심사에서 자주 나오는 질문

**얼마나 걸리나요** — 보통 며칠에서 2주 사이입니다. 몇 주가 걸리기도 합니다.

**글이 몇 개 있어야 하나요** — 구글이 공식적으로 정한 최소 개수는 없습니다. 다만
실무적으로는 독립적인 콘텐츠 페이지가 20~30개 이상일 때 통과율이 눈에 띄게 올라갑니다.

**거절되면 어떻게 하나요** — 거절 사유를 고친 뒤 재신청할 수 있으며 횟수 제한은
없습니다. 다만 같은 상태로 반복 신청하면 같은 결과가 나옵니다. «가치 있는 콘텐츠
없음» 이라면 링크를 더 늘리는 것이 아니라 **읽을 글을 추가**해야 합니다.

**외부 링크가 너무 많은데 괜찮나요** — 대가를 받지 않은 편집상의 추천 링크는
문제되지 않습니다. 만약 제휴 수수료를 받는 링크가 생기면 `rel="sponsored"` 를
붙이고 해당 사실을 명시해야 합니다.
